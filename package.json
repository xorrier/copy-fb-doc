{
  "name": "copy-fb-doc",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "icons": "node scripts/generate-icons.mjs",
    "build:zip": "npm run icons && npm run build && cd dist && zip -r ../firestore-copy-json.zip . && cd .. && echo '✓ firestore-copy-json.zip ready'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "typescript": "^6.0.3",
    "vite": "^5.4.21"
  }
}
